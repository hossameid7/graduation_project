from django.apps import AppConfig


class ApiConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "api"

    def ready(self):
        # Initialize chat model when Django starts
        from .chat_model import ChatModel
        ChatModel()
